import { CommonListingTable } from './common-listing-table';

describe('CommonListingTable', () => {
  it('should create an instance', () => {
    expect(new CommonListingTable()).toBeTruthy();
  });
});
