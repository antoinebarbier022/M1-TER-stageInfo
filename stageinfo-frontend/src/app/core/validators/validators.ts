/*************************************/
/** Own validators are created here **/
/*************************************/

import { AbstractControl } from "@angular/forms";

let isStudent = (control: AbstractControl) : {[key: string]: boolean} | null => {

    return null;
}